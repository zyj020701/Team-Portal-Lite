\#项目说明书——（spec d23.md）性能优化日（Day 23）

\# 项目信息

\- 项目中文名：团队内部门户・简易版

\- 项目英文代号：team-portal-lite

\- 项目定位：内部教学练习项目（非商用），用于演练前端性能优化全流程

\#一. 项目目的

前天（Day 21）搭好了毛坯房，昨天（Day 22）装好家具和水电，公告板已经能跑了。但 “能用” 和 “好用” 是两码事。今天的核心目标：给这间屋子做一次全面 “体检” 和提速改造

\- 用 Lighthouse 跑一次 “体检”，搞清楚当前页面在首屏速度（LCP）、操作响应（INP）、页面稳定性（CLS）三个维度上的真实水平。

\- 针对三大指标逐一 “治疗”：让首屏加载更快、让按钮点击更跟手、让页面内容不跳动。

\- 代码 “减负”：去掉没用的代码、按需加载、替换大体量工具库。

\- 装一个 “心率监测仪”（RUM），让真实用户访问时的体验数据能上报回来，以后页面变慢了能第一时间知道。

\###2. 项目功能

今天要产出 6 类具体资产：

\[1] Lighthouse 体检报告：优化前跑一次 Lighthouse，记录 LCP/INP/CLS 三项分数；优化后再跑一次，对比提升幅度。

\[2] LCP 优化（首屏提速）：用next/image替换所有<img>，配置next/font做字体预加载，加 CDN 缓存策略。

\[3] INP 优化（操作跟手）：用useTransition标记 “筛选待办” 为低优先级操作，用debounce优化搜索输入，拆解耗时任务。

\[4] CLS 优化（页面稳定）：所有图片加固定宽高，公告列表加骨架屏占位，字体配置display: swap防止文字跳动。

\[5] Bundle 优化（代码减负）：用next/dynamic做路由代码分割，用@next/bundle-analyzer分析大文件，替换大体量工具库。

\[6] RUM 真实监控（持续监测）：接入web-vitals库，把真实用户访问时的性能数据上报到后端，建立告警机制。

\###3. 核心约束

在 Day 21-22 的约束基础上，Day 23 新增以下性能优化级别的红线：

\[1] 图片必须有宽高：所有<img>或next/image必须设置width和height，或使用fill属性配合父容器。缺一个就算不合格。

\###4. 技术限制

\[1] 禁止直接使用<img>标签：所有图片必须用next/image替代原生<img>，以自动获得懒加载、WebP 转换、占位等功能。

\[2] 字体必须用next/font：所有自定义字体必须next/font加载，不得在 CSS 中用@font-face手动引入。

\[3] 每个页面必须有路由分割：每个page.tsx必须是独立的打包入口，不允许所有页面代码打包成一个文件。

\[4] 监控必须先优化再上报：优化完成后必须接入web-vitals上报，不能只做完 Lighthouse 就跑。

\### 右侧技术细则

图片优化：next/image（Next.js 自带）：自动转 WebP/AVIF、懒加载、设置占位。所有图片必须用它。

字体优化：next/font（Next.js 自带）：自动预加载关键字体、设置回退字体。所有自定义字体必须用它。

代码分割：next/dynamic（Next.js 自带）：只有用户用到某个组件时才加载它的代码。用于弹窗、图表列表等组件。

操作优先级：useTransition（React 自带）：把 “筛选列表”“排序” 等操作标成低优先级，不抢用户输入响应。

防抖 / 节流：自己写useDebounce自定义 Hook；搜索框用防抖（停手 200ms 后才触发），滚动用节流（每 16ms 最多触发一次）。

第二张幻灯片内容

左侧板块

虚拟滚动（必练）

@tanstack/react-virtual 即使数据量小，也必须造 10000 条数据来练习虚拟滚动。这是第 25 天实战项目处理 10 万条工单的必备技能。

性能跑分：Lighthouse（Chrome 自带）浏览器自带的体检工具，给 LCP/INP/CLS 三项打分。

体积分析：@next/bundle-analyzer 打包后看每个文件多大，找到最大的那个重点优化。

真实监控：web-vitals + 自建上报接口，采集用户端 LCP/INP/CLS 数据，上报到后端看 P75/P95 分值。

\###5. 完成步骤（按顺序执行的清单）

步骤 1：跑体检：用 Lighthouse 跑当前页面，记录 LCP/INP/CLS 三项分数和总得分 | 产出一份 “体检报告”，作为优化前基线。

步骤 2：分析工包：安装配置@next/bundle-analyzer，跑一次构建查看当前打包体积 | 找出最大的文件，确定优化目标。

步骤 3：修 LCP：把所有<img>换成next/image，配置next/font，在next.config.js中配置 CDN 远程图片域名 | 图片和字体优化通道，首屏加速 | 图片全部补齐 width/height，给公告列表加骨架屏（Skeleton），字体配置display: swap | 页面内容不跳动，骨架屏占位。

步骤 4：练虚拟滚动（必做）：造 10000 条待办数据，用@tanstack/react-virtual实现只渲染可见区 20-30 条 | 滚动顺滑不卡顿，为第 25 天 10 万条工单列表打底。

步骤 6：修 INP：给 “筛选待办” 加useTransition，给搜索框加防抖（debounce），确保操作响应＜200ms | 操作跟手，点筛选不卡、打字不频繁触发搜索。

步骤 7：修 Bundle：用next/dynamic按需加载、路由分割，用 Bundle Analyzer 找到大库并替换（如 moment→dayjs），启用 Tree Shaking | 打包体积变小，首屏加载变快。

步骤 8：再跑一次 Lighthouse：验证优化效果，记录优化后的分数 | 产出 “优化后报告”，对比前后差异。

步骤 9：接 RUM：安装web-vitals，在layout.tsx中监听 LCP/INP/CLS，上报到自建/api/vitals接口 | 真实用户数据能持续上报和监控。

\#6. 验收标准

\[1] LCP 达标 | Lighthouse 跑分显示 LCP ＜2.5s

\[2] INP 达标 | Lighthouse 跑分显示 INP ＜200ms（或原 FID＜100ms）

\[3] CLS 达标 | Lighthouse 跑分显示 CLS ＜0.1

右侧板块

\[4] 总得分达标 | Lighthouse Performance 总分＞90

\[5] 所有图片用 next/image | 全局搜索<img>标签数量为 0

\[6] 所有字体用 next/font | 全局搜索@font-face数量为 0

\[7] Bundle 体积达标 | 首屏 JS 总大小＜200KB（gzip 后）

\[8] RUM 已接入 | 访问页面后，能看到性能数据上报到后端（检查/api/vitals是否收到数据）

\[9] 骨架屏生效 | 刷新页面时，公告列表位置先显示灰色占位条，再显示真实内容

\###7. 常见陷阱

陷阱 1：只优化 Lighthouse，不管真实用户

在开发者电脑上（网速快、设备好）跑 Lighthouse 得了 95 分，但用户用的老旧手机 + 2G 网络打开依然卡顿。

Lighthouse 是 “实验室数据”，不是 “真实体验”。必须同时接 RUM（web-vitals），看真实用户的 P75/P95 数据。两者都达标才算真优化。

陷阱 2：INP 优化只管 “点击”，不管 “输入”

觉得 INP 只管按钮点击，忽略了搜索框输入、下拉框切换这些操作。用户打字卡顿也是 INP 的一部分。所有交互操作（点击、输入、滚动、选择）都要测，尤其是输入框必须加 debounce。

Tree Shaking 摇树失效，冗余代码依旧打包

误区3：引入 ESM 模块就默认开启摇树；

规避：package.json 配置"sideEffects": false，全程使用 import 语法，杜绝 require 混用。

字体优化配置不完整

误区4：仅引入 next/font，未配置 display:swap；

规避：必须开启字体回落，避免字体加载空白（FOIT）问题。

骨架屏与真实内容尺寸不一致

误区5：骨架屏宽高、边距和最终渲染内容存在偏差；

规避：骨架屏与真实内容复用同一套 CSS 样式，尺寸完全统一，杜绝加载后页面跳动。

数据量偏少强行接入虚拟滚动

误区6：列表仅有十几条数据就使用虚拟滚动；

规避：列表数据≥100\~200 条才启用虚拟滚动，少量数据使用普通渲染即可。

接入监控但不查看、不配置告警

误区7：完成性能数据上报后长期无人查看日志；

规避：设置告警规则，当线上用户 P75 档位 LCP 超过 2.5s 时，自动推送钉钉 / 邮件告警。

