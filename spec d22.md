\#项目规格说明书 (spec d22.md) —— 组件与状态日志 (Day 22)

项目信息

项目中文名：团队内部门户・简易版

项目英文代号：team-portal-lite

项目定位：内部教学练习项目（非商用），用于演练高级组件模式与状态管理架构。

\#一. 项目目的

昨天我们画好了图纸、打好了地基、定好了规矩。今天的核心目标是：开始造家具（写组件）和铺水电（管数据）。

造一批通用的、可复用的界面零件（公告卡片、待办项、头像），让后续所有页面直接拿来用。

铺一套清晰的数据管理方案（用户状态、公告数据、待办数据、筛选状态），让数据在不同页面间畅通无阻。

确保所有零件都陈列在 Storybook 里，方便查看和测试。

\#二. 项目功能

今天要产出 4 类具体资产：

\[1] 3-5 个通用界面组件：公告卡片、待办项清单、头像组件、标签页切换器、按钮（带不同样式）。放在packages/ui里，所有页面共用。

\[2] 组件展厅（Storybook）：一个独立页面，把所有组件陈列出来，能单独看每个组件的不同状态（正常、加载中、已完成、置灰等）。

\[3] 全局状态（Zustand）：管住 “当前登录用户是谁”，放在packages/store里，整个项目都能调取。

\[4] 服务端数据管理（TanStack Query）：负责 “公告列表” 和 “待办列表” 的获取、缓存、变更。页面一打开自动拿数据，5 分钟内重复打开不重复请求。

\[5] URL 状态管理（nuqs）：把待办列表的 “筛选条件”（如 “只看未完成”）存进网址，刷新不丢，复制链接发给同事打开看到的是同一筛选状态。

\#三. 核心约束

在 Day21 的三条约束（单向楼梯、模块独立、页面要薄）基础上，Day22 新增以下组件级别的约束：

\[1] 纯展示原则：通用组件（如公告卡片）只负责 “显示”，不负责调取任何后端接口，数据必须由上层（页面层或业务层）传进来。

\#4. 规范约束

\[1] 单一职责原则：一个组件只做一件事。按钮只负责 “点击响应”，不负责 “判断用户有没有权限”；卡片只负责 “显示内容”，不负责 “决定显示哪条内容”。

\[2] 状态分类归位：不同 “性质” 的数据用不同的工具管：

全局的 “用户登录态” 用 Zustand；

从后端拿来的 “公告列表” 用 TanStack Query；

URL 里的 “筛选条件” 用 nuqs。不许混用。

\[3] 组件不依赖具体业务：packages/ui里的组件不允许引用packages/features里的任何代码。通用组件就是通用组件，不能认识 “公告” 或 “待办” 这些具体业务概念。

\[4] 样式随组件走：所有用 shadcn 生成的组件，样式必须通过 Tailwind 类名控制。禁止在组件内写内联样式（style={}）或单独的.css文件覆盖样式。

\#四. 技术限制

全局状态：Zustand｜管 “当前用户” 这类应用级数据。像手机的 “全局设置”，一个地方改了，所有页面跟着变。

服务端数据：TanStack Query｜管 “公告”“待办” 这类从后端拿来数据。自带缓存、自动重试、后台刷新，不用自己写 loading/error 的重复代码。

URL 状态：nuqs | 把 “筛选”“排序” 存进网址。刷新不丢，分享给同事打开同一状态。

样式方案（必配）：Tailwind CSS（带 tailwind.config.js）

必须安装并配置 Tailwind，shadcn 的组件长得漂亮全靠它，不配置 Tailwind，所有组件都是纯白无样式。

组件基础（必配）：Radix UI + shadcn/ui，两者必须搭配用。

Radix 提供 “无头（无样式）” 逻辑（如键盘操作、无障碍），shadcn 提供漂亮的 Tailwind 样式。缺一不可。

组件展厅：Storybook | 每个组件写一个 .stories.tsx 文件，陈列所有组件，方便查看不同状态。

类型安全：TypeScript 严格模式 | 所有组件的 props（传入参数）必须有类型定义，不允许用 any。

五、完成步骤

01\. 装齐工具箱

任务：在根目录安装 Zustand、TanStack Query、nuqs、Storybook 等依赖包。

验收：所有依赖安装成功，运行各工具的命令不报错

02\. 管登录状态

任务：在 packages/store 里用 Zustand 建 userStore，存用户名并提供登录登出方法。

验收：首页能显示当前用户名，点切换按钮后名字同步更新

03\. 建数据客户端

任务：在 apps/web 里配置 QueryClientProvider，用 Provider 包裹整个应用。

验收：应用内任意组件都能用 useQuery 和 useMutation 取数据

04\. 写取数函数

任务：在 packages/features 里写 useAnnouncements 和 useTodos 两个自定义 Hook。

验收：调用 Hook 能返回公告列表和待办列表数据（先用假数据）

05\. 造通用零件

任务：用 shadcn 做基础，写公告卡片、待办项、头像共 3 个通用组件。

验收：每个组件有完整的 TypeScript 类型定义，无 any 类型

06\. 搭组件展厅

任务：为每个通用组件创建对应的 stories 文件，展示不同状态（正常 / 加载 / 完成）。

验收：运行 pnpm storybook 能在浏览器打开看到所有组件陈列

07\. 接 URL 状态

任务：在待办列表页用 nuqs 把筛选条件存进网址（如？filter=active）。

验收：点击筛选后网址变化，刷新页面后筛选状态保持不变

08\. 组装首页

任务：在 apps/web 首页整合 Zustand 用户名、Query 公告和待办、nuqs 筛选功能。

验收：首页完整显示用户名、公告列表、可筛选的待办列表

六、常见陷阱

01\. 组件写得 “太死”

公告卡片只能显示 “标题 + 时间 + 内容”，换个需求要显示 “作者 + 阅读数” 就得重写组件。

用 “组合模式”（Compound Components）或 “插槽”（Slot）设计组件，让父组件能自由决定 “里面放什么”。

02\. 把服务端数据塞进 Zustand

把从后端拿来的 “公告列表” 存进了 Zustand，导致 TanStack Query 的缓存能力完全失效，每次刷新都得重新请求。

Zustand 只管 “应用全局状态”（如用户登录态），服务端数据（公告 / 待办）留给 TanStack Query 管。

03\. 忘了 URL 状态的重要性

把 “筛选条件” 存在组件内部的 useState 里，结果一刷新页面筛选就丢了，用户得重新选。

凡是涉及 “筛选”“排序”“分页”“搜索关键词” 的，一律用 nuqs 存进 URL。

04\. Storybook 只建不用

建了 Storybook 但只写了 1 个组件的展示，后面积累更多组件时懒得更新，展厅越来越荒废。

每新建一个通用组件，必须同时写对应的.stories.tsx，否则算没做完。

05\. 组件里直接写 “取数” 逻辑

在公告卡片组件内部直接调用fetch('/api/announcements')，导致卡片只能在这个项目里用，换个项目就废了。

组件只接收props（从外部传进来的数据），内部不允许出现任何fetch或useQuery。

